import React from "react"
import classes from './EmptyElement.module.css';

const EmptyElement = (props) => {
  return (
    <div className={classes.emptyDiv}>
    </div>
  )
};

export default EmptyElement;
